# Business Semantic Layer Demo

Static read-only GitHub Pages build for stakeholder feedback.

The source code is maintained separately in the private `business-semantic-layer` repository. This public repo contains only compiled demo artifacts with seeded Player Performance / FOT data and no backend, database, secrets, or regulated data.
